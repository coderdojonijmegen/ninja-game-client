class Viewport {
    constructor(width_, height_, posX_, posY_) {
        this.width = width_;
        this.height = height_;

        this.windowWidth = width_;
        this.windowHeight = height_;

        this.posX = posX_;
        this.posY = posY_;
    }
}